<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
          SM8750 exposes FastRPC through this vendor-provided non-NDK library.
          targetSdk 31+ packages must request it explicitly so the native linker
          namespace can resolve libQnnHtpV79Stub.so -> libcdsprpc.so.
        -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />
    </application>
</manifest>
