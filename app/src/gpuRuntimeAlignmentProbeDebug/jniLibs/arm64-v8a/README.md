# gpuRuntimeAlignmentProbe native libs

This directory is reserved for the DEV-only `gpuRuntimeAlignmentProbeDebug` flavor.

Do not commit native `.so` files here. Use this source set only for local, full-stack
runtime alignment experiments after the candidate stack has been documented.

The `standard` flavor and `standardDebug` native library inputs must not use this
directory.
