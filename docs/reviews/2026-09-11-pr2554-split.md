# PR #2554 replacement map

Base: main 5305eed9. Original candidate: e940c193, preserved on archive/gpu-pr2554-e940c193. Closing the old PR does not discard its branch, experiment artifacts or history.

| Original change | Destination / decision |
| --- | --- |
| LocalStreamingRunner guessed-space removal | PR #2567: fix/litert-streaming-whitespace |
| ChatScreen timeout partial preservation and classification tests | PR #2568: fix/gpu-timeout-partial-preservation, Draft pending normal-UI timeout/recovery evidence |
| Engine default 1024 to 512 and corresponding assertions | Rejected from replacements: confuses context with output-only capacity |
| Benchmark receiver extensions, CPU configuration probes, debug model setup receiver and candidate manifest | test/gpu-context-budget-evidence; diagnostic only |
| Minimal candidate JNI packaging | test/gpu-context-budget-evidence; candidate only, not production promotion |
| Isolated stage script, stale-state regression and CI wiring | test/gpu-context-budget-evidence |
| Historical device reports and per-run evidence | Preserved with corrected context interpretation in test/gpu-context-budget-evidence |

## Compatibility and measurement

The receiver accepts canonical context_max_tokens_list / context_max_tokens_list_base64 before legacy max_output_tokens_list aliases. Legacy Kotlin fields and CSV columns remain for existing controllers. New CSV fields explicitly provide requested_context_tokens, resolved_engine_max_num_tokens and token_budget_semantics. Resolved configuration is not proof that engine creation succeeded. Generated tokens remain in output_tokens only when measured; unknown remains unavailable.

The stage runner sends the canonical context parameter. Existing artifact directory names are retained for automation compatibility; their numeric stage is context capacity. Historical reports are corrected rather than treating previous completion as output-limit qualification.

## Remaining work

PR #2567 needs its hosted CI. PR #2568 additionally needs normal UI real-timeout, cancellation precedence, failure persistence, late callback and next-turn recovery evidence. The diagnostic PR does not raise or lower main context, promise native close recovery, or demonstrate Edge Gallery parity. The original mixed streaming WIP on archive/streaming-wip-20260911 remains a separate, unmerged task.
